pre-push:
  commands:
    lychee:
      tags: links
      run: lychee --no-progress --offline --exclude-path '.agents/skills/dogfood/templates' --exclude-path '.claude/skills/dogfood/templates' '**/*.md'

