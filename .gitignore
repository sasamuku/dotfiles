# This is intended as GLOBAL ~/.gitignore, not just this dotfiles repo. (But.. it's both.)
# See .gitconfig: excludesFile = ~/.gitignore

# Secrets and sensitive files (dotfiles repo specific)
.zsh_secrets
.zshrc.secrets
.gitconfig.local
.env
.env.local

# macOS
.DS_Store

# direnv
.envrc

# Editors and IDEs
.vscode/
.cursor/

# Ruby
*.gem
.pryrc

# Git Worktree Manager hooks
.wt_hook.sh

# Claude
.claude/settings.local.json

